<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Ferma Tech</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }
        .page-header h1 {
            margin: 0;
            font-size: 1.5rem;
        }
        .detail-grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 24px;
        }
        @media (max-width: 900px) {
            .detail-grid {
                grid-template-columns: 1fr;
            }
        }
        .info-card {
            background: #fff;
            border-radius: var(--radius);
            padding: 24px;
            box-shadow: var(--shadow);
        }
        .info-card h3 {
            margin: 0 0 16px;
            font-size: 1rem;
            color: var(--green-700);
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #f1f3f5;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .info-label {
            color: #6c757d;
            font-size: 0.9rem;
        }
        .info-value {
            font-weight: 600;
            text-align: right;
        }
        .data-table {
            width: 100%;
            border-collapse: collapse;
        }
        .data-table th,
        .data-table td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid #e5eef0;
        }
        .data-table th {
            background: #f8fafb;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #6c757d;
        }
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        .badge-admin {
            background: #dbeafe;
            color: #1d4ed8;
        }
        .badge-member {
            background: #dcfce7;
            color: #166534;
        }
        .badge-active {
            background: #dcfce7;
            color: #166534;
        }
        .badge-inactive {
            background: #fee2e2;
            color: #991b1b;
        }
        .btn-sm {
            padding: 6px 12px;
            font-size: 0.85rem;
        }
        .btn-danger {
            background: #dc2626;
            color: #fff;
            border: none;
        }
        .btn-danger:hover {
            background: #b91c1c;
        }
        .btn-group {
            display: flex;
            gap: 8px;
        }
        .alert {
            padding: 14px 16px;
            border-radius: 12px;
            margin-bottom: 20px;
            font-size: 0.9rem;
        }
        .alert-success {
            background: #dcfce7;
            color: #166534;
            border: 1px solid #bbf7d0;
        }
        .action-buttons {
            display: flex;
            gap: 12px;
            margin-bottom: 24px;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <nav class="sidebar" id="sidebar">
        <div class="brand" style="display: flex; align-items: center;">
            <button class="close-sidebar" aria-label="Close navigation" style="background: transparent; border: none; cursor: pointer; margin-right: 10px; position: relative;">
                <span style="display: block; width: 20px; height: 2px; background: black; transform: rotate(45deg); position: absolute; top: 50%; left: 50%; transform-origin: center; margin: -1px 0 0 -10px;"></span>
                <span style="display: block; width: 20px; height: 2px; background: black; transform: rotate(-45deg); position: absolute; top: 50%; left: 50%; transform-origin: center; margin: -1px 0 0 -10px;"></span>
            </button>
            <img src="/images/icons/logo.png" alt="Ferma Tech Logo">
            <h1>Ferma Tech</h1>
        </div>
        <ul class="nav">
            <li><a href="/">Dashboard</a></li>
            <li><a href="/admin">Super Admin</a></li>
            <li><a href="/admin/communities" class="active">Communities</a></li>
            <li><a href="/admin/users">Users</a></li>
            <li><a href="/settings">Settings</a></li>
        </ul>
        <footer>
            <span class="tag">
                <span class="dot"></span> <%= user.firstName %> (SuperAdmin)
            </span>
        </footer>
    </nav>

    <main class="content">
        <div class="page-header">
            <h1><%= community.name %></h1>
            <a href="javascript:history.back()" class="btn muted">← Back</a>
        </div>

        <div class="action-buttons">
            <a href="/admin/communities/<%= community._id %>/edit" class="btn primary">Edit Community</a>
            <form method="POST" action="/admin/communities/<%= community._id %>/delete" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this community? This action cannot be undone.');">
                <button type="submit" class="btn btn-danger">Delete Community</button>
            </form>
        </div>

        <div class="detail-grid">
            <div>
                <div class="info-card" style="margin-bottom: 24px;">
                    <h3>Community Details</h3>
                    
                    <div class="info-row">
                        <span class="info-label">Name</span>
                        <span class="info-value"><%= community.name %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Slug</span>
                        <span class="info-value"><code><%= community.slug %></code></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Status</span>
                        <span class="info-value">
                            <span class="badge <%= community.isActive ? 'badge-active' : 'badge-inactive' %>">
                                <%= community.isActive ? 'Active' : 'Inactive' %>
                            </span>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Owner</span>
                        <span class="info-value">
                            <% if (community.owner) { %>
                                <%= community.owner.firstName %> <%= community.owner.lastName %>
                            <% } else { %>
                                N/A
                            <% } %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Created</span>
                        <span class="info-value"><%= new Date(community.createdAt).toLocaleDateString() %></span>
                    </div>
                </div>

                <div class="info-card">
                    <h3>Contact Information</h3>
                    
                    <div class="info-row">
                        <span class="info-label">Phone</span>
                        <span class="info-value"><%= community.contactPhone || '—' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Email</span>
                        <span class="info-value"><%= community.contactEmail || '—' %></span>
                    </div>
                    <% if (community.address) { %>
                    <div class="info-row">
                        <span class="info-label">Address</span>
                        <span class="info-value" style="text-align: right;">
                            <% if (community.address.street) { %><%= community.address.street %><br><% } %>
                            <% if (community.address.city || community.address.state) { %>
                                <%= community.address.city %><%= community.address.city && community.address.state ? ', ' : '' %><%= community.address.state %> <%= community.address.postalCode %><br>
                            <% } %>
                            <%= community.address.country || '' %>
                        </span>
                    </div>
                    <% } %>
                </div>
            </div>

            <div>
                <div class="info-card">
                    <h3>Members (<%= members.length %>)</h3>
                    
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Role</th>
                                <th>Joined</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (members.length === 0) { %>
                                <tr>
                                    <td colspan="4" style="text-align: center; color: #6c757d; padding: 24px;">
                                        No members yet
                                    </td>
                                </tr>
                            <% } else { %>
                                <% members.forEach(function(m) { %>
                                    <tr>
                                        <td>
                                            <a href="/admin/users/<%= m._id %>">
                                                <%= m.firstName %> <%= m.lastName %>
                                            </a>
                                        </td>
                                        <td><%= m.email %></td>
                                        <td>
                                            <span class="badge <%= m.communityRole === 'Admin' ? 'badge-admin' : 'badge-member' %>">
                                                <%= m.communityRole %>
                                            </span>
                                        </td>
                                        <td><%= m.joinedAt ? new Date(m.joinedAt).toLocaleDateString() : '—' %></td>
                                    </tr>
                                <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script>
        document.querySelector('.hamburger').addEventListener('click', function() {
            document.getElementById('sidebar').classList.add('open');
        });
        document.querySelector('.close-sidebar').addEventListener('click', function() {
            document.getElementById('sidebar').classList.remove('open');
        });
    </script>
</body>
</html>
