<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Ferma Tech</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .admin-header {
            background: linear-gradient(120deg, #6f42c1, #9333ea);
            color: white;
            padding: 24px;
            border-radius: var(--radius);
            margin-bottom: 24px;
        }
        .admin-header h1 {
            margin: 0 0 8px;
            font-size: 1.75rem;
        }
        .admin-header p {
            margin: 0;
            opacity: 0.9;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 32px;
        }
        .stat-card {
            background: #fff;
            border-radius: var(--radius);
            padding: 24px;
            box-shadow: var(--shadow);
            text-align: center;
        }
        .stat-card .stat-value {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--green-700);
        }
        .stat-card .stat-label {
            color: #6c757d;
            font-size: 0.9rem;
            margin-top: 4px;
        }
        .section-card {
            background: #fff;
            border-radius: var(--radius);
            padding: 24px;
            box-shadow: var(--shadow);
            margin-bottom: 24px;
        }
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .section-header h2 {
            margin: 0;
            font-size: 1.25rem;
        }
        .data-table {
            width: 100%;
            border-collapse: collapse;
        }
        .data-table th,
        .data-table td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid #e5eef0;
        }
        .data-table th {
            background: #f8fafb;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #6c757d;
        }
        .data-table tbody tr:hover {
            background: #f8fafb;
        }
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        .badge-admin {
            background: #dbeafe;
            color: #1d4ed8;
        }
        .badge-member {
            background: #dcfce7;
            color: #166534;
        }
        .badge-superadmin {
            background: #f3e8ff;
            color: #7c3aed;
        }
        .badge-active {
            background: #dcfce7;
            color: #166534;
        }
        .badge-inactive {
            background: #fee2e2;
            color: #991b1b;
        }
        .btn-sm {
            padding: 6px 12px;
            font-size: 0.85rem;
        }
        .quick-actions {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <nav class="sidebar" id="sidebar">
        <div class="brand" style="display: flex; align-items: center;">
            <button class="close-sidebar" aria-label="Close navigation" style="background: transparent; border: none; cursor: pointer; margin-right: 10px; position: relative;">
                <span style="display: block; width: 20px; height: 2px; background: black; transform: rotate(45deg); position: absolute; top: 50%; left: 50%; transform-origin: center; margin: -1px 0 0 -10px;"></span>
                <span style="display: block; width: 20px; height: 2px; background: black; transform: rotate(-45deg); position: absolute; top: 50%; left: 50%; transform-origin: center; margin: -1px 0 0 -10px;"></span>
            </button>
            <img src="/images/icons/logo.png" alt="Ferma Tech Logo">
            <h1>Ferma Tech</h1>
        </div>
        <ul class="nav">
            <li><a href="/">Dashboard</a></li>
            <li><a href="/admin" class="active">Super Admin</a></li>
            <li><a href="/admin/communities">Communities</a></li>
            <li><a href="/admin/users">Users</a></li>
            <li><a href="/settings">Settings</a></li>
        </ul>
        <footer>
            <span class="tag">
                <span class="dot"></span> <%= user.firstName %> (SuperAdmin)
            </span>
        </footer>
    </nav>

    <main class="content">
        <div class="admin-header">
            <h1>Super Admin Dashboard</h1>
            <p>Manage all communities, users, and platform settings</p>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-value"><%= stats.totalCommunities %></div>
                <div class="stat-label">Total Communities</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= stats.totalUsers %></div>
                <div class="stat-label">Total Users</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= stats.activeUsers %></div>
                <div class="stat-label">Active Users</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= stats.superAdmins %></div>
                <div class="stat-label">Super Admins</div>
            </div>
        </div>

        <div class="quick-actions" style="margin-bottom: 24px;">
            <a href="/admin/communities/create" class="btn primary">+ Create Community</a>
            <a href="/admin/users/create" class="btn primary">+ Create User</a>
        </div>

        <div class="section-card">
            <div class="section-header">
                <h2>Recent Communities</h2>
                <a href="/admin/communities" class="btn muted btn-sm">View All</a>
            </div>
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Owner</th>
                        <th>Members</th>
                        <th>Status</th>
                        <th>Created</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (communities.length === 0) { %>
                        <tr>
                            <td colspan="6" style="text-align: center; color: #6c757d;">No communities yet</td>
                        </tr>
                    <% } else { %>
                        <% communities.slice(0, 5).forEach(function(c) { %>
                            <tr>
                                <td><strong><%= c.name %></strong></td>
                                <td><%= c.owner ? c.owner.firstName + ' ' + c.owner.lastName : 'N/A' %></td>
                                <td><%= c.memberCount %></td>
                                <td>
                                    <span class="badge <%= c.isActive ? 'badge-active' : 'badge-inactive' %>">
                                        <%= c.isActive ? 'Active' : 'Inactive' %>
                                    </span>
                                </td>
                                <td><%= new Date(c.createdAt).toLocaleDateString() %></td>
                                <td style="display: flex; gap: 8px;">
                                    <a href="/admin/communities/<%= c._id %>" class="btn muted btn-sm">View</a>
                                    <form method="POST" action="/admin/impersonate/<%= c._id %>" style="margin: 0;">
                                        <button type="submit" class="btn primary btn-sm" title="View as owner">👁️ Enter</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="section-card">
            <div class="section-header">
                <h2>Recent Users</h2>
                <a href="/admin/users" class="btn muted btn-sm">View All</a>
            </div>
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Status</th>
                        <th>Last Login</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (users.length === 0) { %>
                        <tr>
                            <td colspan="6" style="text-align: center; color: #6c757d;">No users yet</td>
                        </tr>
                    <% } else { %>
                        <% users.slice(0, 5).forEach(function(u) { %>
                            <tr>
                                <td><strong><%= u.firstName %> <%= u.lastName %></strong></td>
                                <td><%= u.email %></td>
                                <td>
                                    <span class="badge <%= u.role === 'SuperAdmin' ? 'badge-superadmin' : 'badge-member' %>">
                                        <%= u.role %>
                                    </span>
                                </td>
                                <td>
                                    <span class="badge <%= u.isActive ? 'badge-active' : 'badge-inactive' %>">
                                        <%= u.isActive ? 'Active' : 'Inactive' %>
                                    </span>
                                </td>
                                <td><%= u.lastLogin ? new Date(u.lastLogin).toLocaleDateString() : 'Never' %></td>
                                <td>
                                    <a href="/admin/users/<%= u._id %>" class="btn muted btn-sm">View</a>
                                </td>
                            </tr>
                        <% }); %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script>
        // Sidebar toggle
        document.querySelector('.hamburger').addEventListener('click', function() {
            document.getElementById('sidebar').classList.add('open');
        });
        document.querySelector('.close-sidebar').addEventListener('click', function() {
            document.getElementById('sidebar').classList.remove('open');
        });
    </script>
</body>
</html>
